;(ns crate
  ;(:require [crate.api.database :as database]
            ;[path]))

;(defn rel [args] (apply path.join __dirname args))

;(database.connect! 27017)

;(server 4000
  ;(page "/" (rel "ui" "track-library.wisp"))
  ;(database.route-search      "/search")
  ;(database.route-analyze-key "/analyze-key")
  ;(database.route-analyze-bpm "/analyze-bpm"))

(ns crate)

(console.log "BOEPRIPASI")
